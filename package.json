{
  "scripts": {
    "lint": "npx cfn-lint validate",
    "deploy": "npx sls deploy",
    "remove": "npx sls remove",
    "invoke": "npx sls invoke -f"
  },
  "devDependencies": {
    "cfn-lint": "^1.9.7",
    "serverless": "^1.47.0"
  }
}
